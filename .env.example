# Database — update password before first run
DATABASE_URL=postgresql://finance:changeme@localhost:5433/finance
POSTGRES_PASSWORD=changeme

# Session — how long before a login expires (hours); default 720 = 30 days
SESSION_EXPIRE_HOURS=720

# Rate limiting on /api/auth/login
LOGIN_RATE_LIMIT=10/minute
